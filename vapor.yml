id: 49219
name: UTM-SAMS
environments:
    production:
        storage: utmsamsbucket
        memory: 1024
        runtime: 'php-8.2:al2'
        database: utm-sams-db
        build:
            - 'php artisan build:composer-mirror'
            - 'composer install --no-dev'
            - 'php artisan event:cache'
            - 'npm ci && npm run build && rm -rf node_modules'
        deploy:
            # - 'php artisan migrate:fresh --force'
